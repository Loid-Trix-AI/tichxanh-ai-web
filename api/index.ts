// This file is no longer needed — the app runs as a static SPA.
// All routing is handled client-side by TanStack Router.
// Vercel serves dist/index.html for all paths via vercel.json rewrites.
export {};
